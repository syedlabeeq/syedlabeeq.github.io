# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*-dist
build

# Vite / tooling caches
.vite
node_modules/.vite
*.tsbuildinfo
.eslintcache

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
pids

# Environment / secrets
.env
.env.*
!.env.example
*.local

# Editors
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Note: these are generated by `npm run generate` (which `prebuild` runs), but
# they are kept in git so the dev server and a fresh clone work without a
# GitHub token:
#   public/manifest.json
#   public/rss.xml
#   public/blog/**                   (per-post share pages)
#   public/contributions/index.json
